import { ColorNoteDirective } from './color-note.directive';

describe('ColorNoteDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorNoteDirective();
    expect(directive).toBeTruthy();
  });
});
